import 'package:flutter/material.dart';

class ActivityInvPage extends StatelessWidget {
  const ActivityInvPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('data'),
    );
  }
}
